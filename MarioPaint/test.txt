//FOR TESTING PURPOSES
void printOtherFile(string file_name) {
    ifstream file;
    file.open(file_name, ios::binary); //open a new file stream
    string line;
    //loop through each line
    while(getline(file,line)) {
        cout << line << "\n";
    }
    file.close(); //close when finished
}
